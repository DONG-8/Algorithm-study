id_list = ["muzi", "frodo", "apeach", "neo"];
report = ["muzi frodo", "apeach frodo", "frodo neo", "muzi neo", "apeach muzi"];
k = 2;

function solution(id_list, report, k) {
  let arr = {};
  array.forEach((element) => {
    console.log(element);
    arr.push({ element: [] });
  });
  var answer = [];
  return answer;
}
// 한번에 한명의 유저만 신고할 수 있습니다.

solution(id_list, report, k);
