// 없는숫자 더하기
// 배열에서 없는 숫자만의 list를 추출하여 더해준다.

function solution(numbers) {
  const result = numbers.re;
  return numbers.reduce((acc, cur) => {
    return acc - cur;
  }, 45);
}

arr = [1, 2, 3, 4, 6, 7, 8, 0];
console.log(solution(arr));
